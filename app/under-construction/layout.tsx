
    export default function UnderConstructionLayout({children}: {children: React.ReactNode}) {
    // This layout is intentionally empty to allow the under-construction page to be displayed        
        return <>{children}</>;
    }